@page
@model OrderEntrySystem.Web.Pages.Customers.IndexModel
@{
    ViewData["Title"] = "Customers";
}

@if (!string.IsNullOrEmpty(Model.StatusMessage))
{
    <div class="alert alert-success" role="alert">
        @Model.StatusMessage
    </div>
}

<div>
    <h1>Customers <span class="badge bg-secondary rounded-pill">@Model.Customers.Count()</span></h1>
</div>

<table class="table">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
            <th>Address</th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        @foreach (var p in Model.Customers)
        {
            <tr>
                <td><input type="checkbox" class="row-select" value="@p.Id" /></td>
                <td>@p.Name</td>
                <td>@p.Address</td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@p.Id" class="btn btn-sm btn-outline-secondary">Edit</a>
                    <form asp-page="./Index" asp-page-handler="Delete" asp-route-id="@p.Id"
                          method="post" style="display:inline"
                          onsubmit="return confirm('Are you sure you want to delete this customer?');">
                        <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

<div>
    Customers Selected: <span id="select-count">0</span>
</div>